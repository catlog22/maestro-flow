// Maestro vendors the Impeccable core and exposes it through one canonical
// command instead of a separately installed upstream skill.
export const IMPECCABLE_COMMAND_PREFIX = "/";
export const IMPECCABLE_PROVIDER_ID = "maestro";
export const IMPECCABLE_COMMAND = `${IMPECCABLE_COMMAND_PREFIX}maestro-impeccable`;
